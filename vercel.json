{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node",
      "config": {
        "zeroConfig": true,
        "buildCommand": "npm run build",
        "outputDirectory": "dist"
      }
    }
  ]
}