// src/context/SupabaseContext.js
import { createContext } from 'react';

/**
 * Context for Supabase authentication and data access
 */
const SupabaseContext = createContext(null);

export default SupabaseContext;
