// src/context/WebSocketContext.js - WebSocket context definition only
import { createContext } from 'react';

// Create WebSocket context
export const WebSocketContext = createContext(null);

export default WebSocketContext;